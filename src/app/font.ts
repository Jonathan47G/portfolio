import { Anek_Bangla, Scada } from 'next/font/google'

export const bangla = Anek_Bangla({ subsets: ['latin'], weight: "800" });
export const scada = Scada({
    subsets: ['latin'],
    weight: "400"
});